# Web Framework
Flask==3.0.0
Flask-SocketIO==5.3.5
Flask-CORS==4.0.0
python-socketio==5.10.0
python-engineio==4.8.0

# Database
SQLAlchemy==2.0.23
psycopg[binary]>=3.2.0
alembic==1.13.0

# Agent Framework (let pip resolve compatible versions)
langgraph
langchain
langchain-core
langchain-openai

# Data Processing
pandas>=2.2.0
numpy>=1.26.4
scipy>=1.14.0

# Web Scraping
beautifulsoup4==4.12.2
requests==2.31.0
# lxml removed - incompatible with Python 3.14, using html.parser instead

# Visualization
plotly==5.18.0

# Utilities
python-dotenv==1.0.0
pydantic>=2.0.0  # Let pip resolve compatible version

# OpenAI (2.16.0+ required for Responses API with gpt-5-nano)
openai>=2.16.0

# Development
pytest==7.4.3
pytest-cov==4.1.0
black==23.12.0
flake8==6.1.0

# Monitoring (optional)
sentry-sdk[flask]==1.39.1

# Scheduling (for future updates)
APScheduler==3.10.4

# Production server
gunicorn>=21.0.0
gevent>=23.0.0
gevent-websocket>=0.10.1
sqlparse>=0.4.4
